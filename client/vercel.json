{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    {
      "comment": "SPA fallback: react-router owns /dashboard, /transparency, /report — without this a hard refresh on those paths 404s on Vercel.",
      "source": "/((?!api/).*)",
      "destination": "/index.html"
    }
  ]
}
